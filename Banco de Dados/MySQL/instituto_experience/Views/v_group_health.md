---
tipo: view
definer: "root@%"
security_type: "DEFINER"
colunas: 23
tags: [view]
---

# v_group_health

## Propriedades

| Propriedade | Valor |
|---|---|
| Definer | root@% |
| Atualizável | YES |
| Security type | DEFINER |
| Colunas | 23 |

## Lê de
[[telegram_groups_metadata]], [[telegram_team_members]]

## Lida por
—

## Definição SQL

```sql
with `group_base` as (select `b`.`chat_id` AS `chat_id`,`m`.`group_name` AS `group_name`,`m`.`tier` AS `tier`,`m`.`partners` AS `partners`,`m`.`status` AS `group_status`,count(0) AS `total_msgs`,sum((case when (`t`.`user_id` is not null) then 1 else 0 end)) AS `team_msgs`,sum((case when (`t`.`user_id` is null) then 1 else 0 end)) AS `affiliate_msgs`,count(distinct (case when (`t`.`user_id` is null) then `b`.`message_sent_at_date` else NULL end)) AS `affiliate_active_days`,greatest(1,(to_days(curdate()) - to_days(min(`b`.`message_sent_at_date`)))) AS `total_days`,max((case when (`t`.`user_id` is null) then `b`.`message_sent_at_date` else NULL end)) AS `last_affiliate_msg_date`,max(`b`.`message_sent_at_date`) AS `last_msg_date`,min(`b`.`message_sent_at_date`) AS `first_msg_date` from ((`instituto_experience`.`telegram_groups_backup` `b` join `instituto_experience`.`telegram_groups_metadata` `m` on((`b`.`chat_id` = `m`.`chat_id`))) left join `instituto_experience`.`telegram_team_members` `t` on(((`b`.`user_id` = `t`.`user_id`) and (`t`.`is_active` = 1)))) group by `b`.`chat_id`,`m`.`group_name`,`m`.`tier`,`m`.`partners`,`m`.`status`), `group_scores` as (select `gb`.`chat_id` AS `chat_id`,`gb`.`group_name` AS `group_name`,`gb`.`tier` AS `tier`,`gb`.`partners` AS `partners`,`gb`.`group_status` AS `group_status`,`gb`.`total_msgs` AS `total_msgs`,`gb`.`team_msgs` AS `team_msgs`,`gb`.`affiliate_msgs` AS `affiliate_msgs`,`gb`.`affiliate_active_days` AS `affiliate_active_days`,`gb`.`total_days` AS `total_days`,`gb`.`last_affiliate_msg_date` AS `last_affiliate_msg_date`,`gb`.`last_msg_date` AS `last_msg_date`,`gb`.`first_msg_date` AS `first_msg_date`,round(((`gb`.`affiliate_active_days` / `gb`.`total_days`) * 100),1) AS `engagement_pct`,coalesce((to_days(curdate()) - to_days(`gb`.`last_affiliate_msg_date`)),999) AS `days_since_last_aff_msg`,coalesce((to_days(curdate()) - to_days(`gb`.`last_msg_date`)),999) AS `days_since_last_msg`,least(100,greatest(0,(case when (coalesce((to_days(curdate()) - to_days(`gb`.`last_affiliate_msg_date`)),999) <= 0) then 0 when (coalesce((to_days(curdate()) - to_days(`gb`.`last_affiliate_msg_date`)),999) <= 3) then round((coalesce((to_days(curdate()) - to_days(`gb`.`last_affiliate_msg_date`)),999) * 6.67),0) when (coalesce((to_days(curdate()) - to_days(`gb`.`last_affiliate_msg_date`)),999) <= 7) then round((20 + ((coalesce((to_days(curdate()) - to_days(`gb`.`last_affiliate_msg_date`)),999) - 3) * 5)),0) when (coalesce((to_days(curdate()) - to_days(`gb`.`last_affiliate_msg_date`)),999) <= 14) then round((40 + ((coalesce((to_days(curdate()) - to_days(`gb`.`last_affiliate_msg_date`)),999) - 7) * 2.86)),0) when (coalesce((to_days(curdate()) - to_days(`gb`.`last_affiliate_msg_date`)),999) <= 21) then round((60 + ((coalesce((to_days(curdate()) - to_days(`gb`.`last_affiliate_msg_date`)),999) - 14) * 2.86)),0) when (coalesce((to_days(curdate()) - to_days(`gb`.`last_affiliate_msg_date`)),999) <= 30) then round((80 + ((coalesce((to_days(curdate()) - to_days(`gb`.`last_affiliate_msg_date`)),999) - 21) * 2.22)),0) else 100 end))) AS `days_score`,least(100,greatest(0,(case when (round(((`gb`.`affiliate_active_days` / `gb`.`total_days`) * 100),1) >= 50) then 0 when (round(((`gb`.`affiliate_active_days` / `gb`.`total_days`) * 100),1) >= 40) then 20 when (round(((`gb`.`affiliate_active_days` / `gb`.`total_days`) * 100),1) >= 30) then 40 when (round(((`gb`.`affiliate_active_days` / `gb`.`total_days`) * 100),1) >= 20) then 60 when (round(((`gb`.`affiliate_active_days` / `gb`.`total_days`) * 100),1) >= 10) then 80 else 100 end))) AS `engagement_inv_score`,(case when (coalesce((to_days(curdate()) - to_days(`gb`.`last_affiliate_msg_date`)),999) <= 0) then 0 else least(100,greatest(0,(case when (`gb`.`total_msgs` >= 500) then 100 when (`gb`.`total_msgs` >= 300) then 80 when (`gb`.`total_msgs` >= 100) then 60 when (`gb`.`total_msgs` >= 50) then 40 when (`gb`.`total_msgs` >= 20) then 20 else 5 end))) end) AS `volume_alarm_score` from `group_base` `gb`) select `gs`.`chat_id` AS `chat_id`,`gs`.`group_name` AS `group_name`,`gs`.`tier` AS `tier`,`gs`.`partners` AS `partners`,`gs`.`group_status` AS `group_status`,`gs`.`total_msgs` AS `total_msgs`,`gs`.`team_msgs` AS `team_msgs`,`gs`.`affiliate_msgs` AS `affiliate_msgs`,`gs`.`affiliate_active_days` AS `affiliate_active_days`,`gs`.`total_days` AS `total_days`,`gs`.`last_affiliate_msg_date` AS `last_affiliate_msg_date`,`gs`.`last_msg_date` AS `last_msg_date`,`gs`.`first_msg_date` AS `first_msg_date`,`gs`.`engagement_pct` AS `engagement_pct`,`gs`.`days_since_last_aff_msg` AS `days_since_last_aff_msg`,`gs`.`days_since_last_msg` AS `days_since_last_msg`,`gs`.`days_score` AS `days_score`,`gs`.`engagement_inv_score` AS `engagement_inv_score`,`gs`.`volume_alarm_score` AS `volume_alarm_score`,round((((`gs`.`days_score` * 0.35) + (`gs`.`engagement_inv_score` * 0.25)) + (`gs`.`volume_alarm_score` * 0.40)),0) AS `risk_score`,(case when (round((((`gs`.`days_score` * 0.35) + (`gs`.`engagement_inv_score` * 0.25)) + (`gs`.`volume_alarm_score` * 0.40)),0) <= 20) then 'Saudável' when (round((((`gs`.`days_score` * 0.35) + (`gs`.`engagement_inv_score` * 0.25)) + (`gs`.`volume_alarm_score` * 0.40)),0) <= 40) then 'Atenção' when (round((((`gs`.`days_score` * 0.35) + (`gs`.`engagement_inv_score` * 0.25)) + (`gs`.`volume_alarm_score` * 0.40)),0) <= 60) then 'Risco' when (round((((`gs`.`days_score` * 0.35) + (`gs`.`engagement_inv_score` * 0.25)) + (`gs`.`volume_alarm_score` * 0.40)),0) <= 80) then 'Crítico' else 'Churn' end) AS `risk_label`,(case when (coalesce((to_days(curdate()) - to_days(`gs`.`last_msg_date`)),999) <= 7) then 'Ativo (7d)' when (coalesce((to_days(curdate()) - to_days(`gs`.`last_msg_date`)),999) <= 14) then 'Ativo (14d)' when (coalesce((to_days(curdate()) - to_days(`gs`.`last_msg_date`)),999) <= 30) then 'Ativo (30d)' else 'Inativo' end) AS `activity_status`,(case when (`gs`.`tier` = 'Tier 1') then 60 when (`gs`.`tier` = 'Tier 2') then 120 else 180 end) AS `sla_target_minutes` from `group_scores` `gs`
```
